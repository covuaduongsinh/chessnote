[package]
name = "chessnote-desktop"
version = "1.1.0"
description = "ChessNote Desktop App"
authors = ["ChessNote Team"]
license = "MIT"
edition = "2021"

[workspace]

[lib]
name = "chessnote_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[[bin]]
name = "chessnote-desktop"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon", "devtools"] }
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"
tauri-plugin-shell = "2"
tauri-plugin-process = "2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
# PDF export (`export_pdf` command): reuses the same headless-Chrome pool the
# server uses for `/.export/pdf`, so a desktop export renders identically to a
# browser export — same code, no bundled server process needed for it.
silverbullet-server-runtime-chrome = { path = "../../server-runtime-chrome" }

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
